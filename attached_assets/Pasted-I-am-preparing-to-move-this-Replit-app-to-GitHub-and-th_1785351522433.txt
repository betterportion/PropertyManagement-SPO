I am preparing to move this Replit app to GitHub and then work on it using Claude Code. I am a beginner.

Before making any changes, perform a thorough read-only audit of the entire project. Do not delete, rewrite, refactor, upgrade, or change any code yet.

Check the following:

1. Security

Search the entire project for exposed API keys, passwords, tokens, private keys, database credentials, service-account files, or other secrets.
Check whether any secrets are hardcoded in source code.
Check whether .env files, secret files, credentials, or sensitive configuration could accidentally be uploaded to GitHub.
Review .gitignore and identify anything that should be added before pushing.
Check authentication, authorization, database rules, API endpoints, user permissions, input validation, and common security risks.
Identify any security issue by severity: Critical, High, Medium, Low, or Informational.

2. Bugs and reliability

Inspect the app for likely bugs, broken features, missing error handling, crashes, invalid assumptions, race conditions, data-loss risks, and edge cases.
Check whether the app handles slow internet, failed API requests, missing data, invalid user input, and unexpected user behavior.
Review database reads and writes for possible duplicate data, failed updates, or inconsistent data.
Check for broken imports, unused code, missing dependencies, incorrect configuration, and build problems.

3. Testing

Identify all existing tests and run them if the project supports testing.
Run the app’s available type checks, linting, build checks, and automated tests.
Do not claim that a feature works unless it was actually tested or verified.
Clearly separate:

Passed
Failed
Not tested
Cannot test in the current environment
Recommend the most important manual tests I should perform myself.

4. Replit-to-GitHub readiness

Identify all Replit-specific files, packages, services, environment variables, deployment settings, or assumptions.
Explain which items are required, optional, or safe to remove later.
Check that the project can be safely copied to GitHub.
Verify that no sensitive files should be committed.
Do not remove Replit-specific code yet.

5. Final report
   Create a file named PRE_GITHUB_AUDIT.md containing:

Executive summary
Security findings
Bug and reliability findings
Test results
GitHub readiness checklist
Replit-specific dependencies
Recommended fixes in priority order
A final status of:

SAFE TO PUSH NOW
SAFE TO PUSH AFTER FIXING SPECIFIC ITEMS
DO NOT PUSH YET

For every finding, include:

Severity
File name and relevant location
What the problem is
Why it matters
Recommended fix

Important:

Do not expose, print, or copy any secret values into the report.
Do not make code changes during this audit.
Ask for my approval before making any fixes.
Be honest about what was and was not actually tested.